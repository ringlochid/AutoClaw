# Definitions

These are bootstrap/import-export definitions for AutoClaw.

- `roles/` — who a node is
- `policies/` — control rules / thresholds
- `workflows/` — reusable graph/subtree templates
- `seeds/` — import helpers / notes

Skills are referenced externally through OpenClaw-managed skill keys/paths.
