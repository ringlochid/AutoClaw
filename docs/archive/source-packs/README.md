# Source packs

Status: Reference

This folder keeps the one retained source pack that still matters for canonical doc provenance.

They are not canonical truth and should be used only after `../../redesign`, `../../current`, repo code/tests, and `..` still leave a gap.

Some archived packs still contain historical references to `rewrite_docs_v1/`.
Treat those references as stale source-pack history; the live canonical root is `../..`.

- `old_version_docs/`

Other older packs were moved to `../legacy-packs`.
