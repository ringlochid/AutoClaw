task:
    key: autoclaw-console-definitions-page
    title: AutoClaw Console Definitions Page
    summary: Build the stored controller-truth definition browser.
    instruction: >-
      Implement the Definitions page as the authoring entrypoint for stored controller
      truth. Use current /definitions route contracts for roles, policies, workflows,
      summaries, filters, search, selection, loading, empty, error, and permission
      states. Keep stored truth separate from draft-set truth, preview truth, diff
      truth, and task-start launch truth. Match references/frontend_design definitions
      anchors without exposing revision history or implementation route nouns as primary
      page semantics unless the contract requires it. Record evidence and ambiguity
      handling under tmp/autoclaw-frontend/60-definitions-page. Include MSW-backed
      tests, view-model coverage, browser evidence, and console validation.
workflow:
    key: frontend-feature-slice
roots:
    workspace:
        mode: use_existing_host
        host_path: /home/ubuntu/leo/projects/autoclaw
    context:
        mode: ensure_host_path
        host_path: /home/ubuntu/leo/projects/autoclaw/tmp/autoclaw-frontend/60-definitions-page
