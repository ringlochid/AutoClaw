task:
    key: autoclaw-console-task-start-page
    title: AutoClaw Console Task Start Page
    summary: Build Task Start from current stored definition truth and task-compose launch contracts.
    instruction: >-
      Implement Task Start as a guarded authoring launch surface from current stored
      controller truth. Use workflow search/selection, task identity fields, root
      binding inputs, preview/start-result disclosure, validation/error handling, and
      POST /tasks/start according to current contracts. Keep task start separate from
      unsaved draft editing and do not launch from draft-only truth. Match
      references/frontend_design task-start anchors and meaningful form states. Record
      evidence and ambiguity handling under tmp/autoclaw-frontend/70-task-start-page.
      Include form, mapper, MSW integration, browser, focus/keyboard, and console
      validation evidence.
workflow:
    key: frontend-feature-slice
roots:
    workspace:
        mode: use_existing_host
        host_path: /home/ubuntu/leo/projects/autoclaw
    context:
        mode: ensure_host_path
        host_path: /home/ubuntu/leo/projects/autoclaw/tmp/autoclaw-frontend/70-task-start-page
